spring:
  application:
    name: student-client
  cloud:
    config:
      uri: http://localhost:8888
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
server:
  servlet:
    context-path: /v1